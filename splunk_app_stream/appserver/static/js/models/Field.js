define([
    "jquery",
    "underscore",
    "backbone"
], function(
    $,
    _,
    Backbone
    ) {
    return Backbone.Model.extend({
        /*initialize: function() {
            this.attributes.aggType = this.attributes.aggType || "";
            this.attributes.desc = this.attributes.desc || "";
            this.attributes.name = this.attributes.name || "";
            this.attributes.term = this.attributes.term || "";
            this.attributes.enabled = this.attributes.enabled || false;
        }*/
    });
});